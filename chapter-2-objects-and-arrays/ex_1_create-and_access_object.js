const movie =
{
    title: "Inception",
    year: 2010,
    rating: 8.8,
    isWatched: true
}

console.log(`The movie ${movie.title} (${movie.year}) has a rating of ${movie.rating}. Watched: ${movie.isWatched}`)